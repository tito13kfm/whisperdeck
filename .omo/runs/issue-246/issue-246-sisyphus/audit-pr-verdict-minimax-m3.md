## PR Audit: #255 fix: _jobFingerprint never includes tagging_job, so tagging progress doesn't repaint   (reviewer: MiniMax M3, independent third family)
PINNED COMMIT 9ee887b4a12835b41158ff69d1f5a0b9ace7981d, not the PR's current tip -- the PR's HEAD at audit time is 7b28be6, which adds a follow-up test rewrite. This audit reviews only the state at 9ee887b.

VERDICT: BLOCK

### Blocking            (empty = none)
- tests/e2e/test_detail_poll_tagging_fingerprint.py:115 the test waits for `#job-tagging` and times out at runtime. No render path in static/rack.js creates that element. Failure scenario: Playwright timeout after 5000ms on `wait_for_selector("#job-tagging")`; the test never reaches the `assert "section 1 of 4" in page.locator("#job-tagging").inner_text().lower()` line below it, so the fingerprint-string change the issue requires is never asserted. Confirmed by running the test: `playwright._impl._errors.TimeoutError: Page.wait_for_selector: Timeout 5000ms exceeded. - waiting for locator("#job-tagging") to be visible`. Fix: rewrite the test to call `_jobFingerprint` directly (already exported on `window` at static/rack.js:5871 in this same pinned tree -- `{ navigate, S, syncTranscribe, renderDetail, curProv, logout, api, loadCostsPage, _jobFingerprint }`) with two payloads differing only in `tagging_job.progress.done` and assert the fingerprints differ. Regression test: `assert window._jobFingerprint({ correction_job:null, summary_job:null, voice_match_job:null, format_markdown_job:null, format_email_job:null, format_coding_prompt_job:null, classify_intent_job:null, tagging_job:{status:'running', progress:{done:1,total:4}} }) !== window._jobFingerprint({ correction_job:null, summary_job:null, voice_match_job:null, format_markdown_job:null, format_email_job:null, format_coding_prompt_job:null, classify_intent_job:null, tagging_job:{status:'running', progress:{done:2,total:4}} })`. This is exactly what the follow-up commit 7b28be6 does, and its message confirms this analysis ("no render path in rack.js creates that element, so the test timed out at runtime").

### Should fix          (empty = none)
- [feature] static/rack.js:4035 references `id="job-tagging"` in `runningContainers` but no render path in static/rack.js creates that element (compare `id="job-correction"` created at line 4276, `id="job-summary"` at 4462, `id="job-voice-match"` at 4653 -- all created in their respective detailBody* functions; no equivalent exists for tagging). Failure scenario: even after the fingerprint fix, `updateDetailJobStatus` correctly detects tagging-only changes but does nothing visible because `$('job-tagging')` returns null. The user's reported symptom (UI never repaints tagging progress) is not actually fixed by this PR's fingerprint change alone -- the deeper bug is a missing widget creation. Fix: add a tagging widget to `renderDetailBody`'s transcript tab (alongside `vm` at line 4653) -- e.g. `const tg = llmJobActive(t.tagging_job) ? '<div id="job-tagging">' + jobRunningUnit(t.tagging_job, 'Tagging') + '</div>' : ''` and prepend `tg +` to the innerHTML at line 4672. Note the issue (#246) itself prescribes only the fingerprint fix, so this is a scope expansion rather than a fidelity issue.

### Nits                (empty = none)
- self-audit.md line numbers (3210 for scheduleDetailPoll guard, 3664 for jobActiveSnapshot) are stale -- the file shifted because the bulk-import feature was merged into master between issue investigation and this audit. The actual lines are 3541 and 3995 in this tree. Substance is correct in both cases; line numbers are not.
- rack.min.js at this pinned SHA already contains `tagging_job` (rebuilt in an earlier merge). The PR's file list reports rack.min.js as modified because that change is in the follow-up commit 7b28be6, NOT in this pinned SHA. The git diff against origin/master at 9ee887b shows no rack.min.js change, which is correct for the pinned snapshot.

### Honesty check
- self-audit.md [x] lines verified: 5/7. False [x] found:
  - Line 17 "[x] A detail-poll test asserting the fingerprint string changes when only tagging_job.progress.done changes -- delivered in test_detail_poll_tagging_fingerprint.py" is FALSE. The test waits for `#job-tagging` (line 115), times out at runtime (verified by running the test), and never asserts on the fingerprint string. The acceptance criterion requires a fingerprint-string assertion; the shipped test provides none.
  - Line 21 "[x] ... mutation check: N/A (e2e browser test, not a unit test with replaceable function body)" is misleading. e2e tests CAN be mutation-checked (revert the `+ '|' + f(t.tagging_job)` line and confirm the test fails -- but it fails for the wrong reason: the timeout, not a fingerprint-string comparison). The "N/A" framing hides that the test cannot pass at all in this state, so the mutation check is undefined.
- Vacuous / loosened tests: the shipped test is effectively vacuous for the fingerprint fix -- it fails 100% of the time regardless of whether the fix is present, because it never reaches the assertions that would exercise the fingerprint. Reverting the `+ '|' + f(t.tagging_job)` line would produce the same timeout failure.
- Undisclosed scope (diff vs claims): the diff is exactly what the PR body describes (one-line fingerprint fix + new test file). No undisclosed scope in the diff itself. Separately, the branch has many commits since master (bulk-import feature, batch-management API, several docs plans) but those are NOT in this PR's diff (master is at 26c0633 here, the fix's parent is 26c0633, so `git diff master...9ee887b` is just the two files listed). I updated my local `master` ref to origin/master before computing the diff.

### Read scope
- Full read on the two changed files (`static/rack.js` change at line 3531, full 148-line new test file). Cost guard not triggered -- diff is 149 insertions total. Also read `scheduleDetailPoll` (lines 3536-3553), `jobActiveSnapshot` (3986-3997), `updateDetailJobStatus` (4004-4045), `renderDetailBody` (4649-4672), and grep-searched for `id="job-*"` in the rest of the static/ tree to verify no widget creation site exists for tagging.

### Summary
The one-line fingerprint fix at static/rack.js:3531 is correct and complete per the issue's prescription, and the sibling sweep is clean (`scheduleDetailPoll` guard at line 3541 and `jobActiveSnapshot` at line 3995 already include `tagging_job`). However, the regression test shipped at this pinned SHA is broken: it waits for a `#job-tagging` DOM element that no render path in rack.js creates, times out after 5 seconds at line 115, and never executes the fingerprint-string assertions the issue's acceptance criterion requires. Running the test confirms the timeout. The follow-up commit 7b28be6 (not part of this pinned SHA) explicitly identifies and fixes this by rewriting the test to assert on `_jobFingerprint()` directly. Without that fix, this PR's claim of regression coverage is false. BLOCK.