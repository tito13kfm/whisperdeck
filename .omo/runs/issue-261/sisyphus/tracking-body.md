**Phase 1 dependency order. Implement in sequence, each as a separate PR.**

- [ ] #???? **Schema + kind plumbing** — database `VoiceDumpItem` table, `voice_dump` kind value, every kind-switch site (`transcription.py`, `queue.py`, `app.py` serialization + bulk import, `settings.py` bulk_defaults, `rack.js` kind picker label only — no new UI yet). Touchpoints: `database/__init__.py`, `services/transcription.py`, `services/llm_jobs.py` (VALID_KINDS + IO_KINDS only, no dispatch yet), `services/settings.py`, `app.py` (`_dictation_job_fields` branch + bulk import validation), `static/rack.js` (kind dropdown option). Dependencies: none.

- [ ] #???? **Backend chain** — `_structure_from_text` refactor from `structure_voice_note`, new `segment_voice_dump`, new `run_voice_dump_job` dispatch in `llm_jobs.py` (wired via `run_llm_job`'s kind switch with `progress_total`). Unit tests: segmentation-call parsing, per-item structure+clarify parsing, truncation fallback. Confirm existing `test_voice_note_chain.py` still passes unchanged. Dependencies: sub-issue 1 (needs `voice_dump` in VALID_KINDS + IO_KINDS).

- [ ] #???? **Endpoints + serialization** — `POST /api/transcripts/{id}/voice-dump/rerun`, `POST /api/transcripts/{id}/voice-dump/save-draft`, `POST /api/transcripts/{id}/voice-dump/finalize`, `GET /api/transcripts/{id}/voice-dump-items`, `GET /api/voice-dump-items`. Auto-enqueue `enqueue_auto_voice_dump` (mirrors `enqueue_auto_voice_note`). `voice_dump_job` serialization field. Route tests mirroring `test_voice_note_route.py`. Dependencies: sub-issues 1+2 (needs kind validation + chain).

- [ ] #???? **Frontend: kind picker + board section** — new board section `loadVoiceDumpItems()` using existing `NOTE_TYPE_LABELS`/`NOTE_TYPE_COLORS`. Kind picker fully wired (value `"voice_dump"` feeds into existing record flow). No inline edit — that's sub-issue 5. Dependencies: sub-issue 3 (needs list endpoint + serialization). Parallelizable with sub-issue 5.

- [ ] #???? **Frontend: Dump Review tab + inline edit** — new detail tab gated on `t.kind === 'voice_dump'`, renders from `t.voice_dump_job.result_json.items`. Per-item: editable title input, editable body textarea, type dropdown (reclassify), discard checkbox, clarifying question prompts + answer inputs. Save-draft button → `save-draft` endpoint. Finalize button → `finalize` endpoint. App's first inline edit pattern — no existing contenteditable in `rack.js`. Dependencies: sub-issue 3 (needs save-draft + finalize endpoints + serialization). Parallelizable with sub-issue 4.
