.venv/
__pycache__/
*.pyc
data/
run_out.log
.superpowers/
run_err.log
test.mp4
test_audio.mp3
.claude/*
!.claude/skills/
!.claude/skills/**
!.claude/commands/
!.claude/commands/**
!.claude/issue-runner-prompt.md
.serena/
*.log
dist/
scripts/.build-cache/
.playwright-mcp/
tests/fixtures/

# Node.js dependencies (esbuild for the frontend build step)
node_modules/

# Local secrets / machine-specific (keep out of git)
scripts/cookies.txt
scripts/providers_backup.json

# UX-audit runtime artifacts (PID file, local config, one-off setup scripts)
scripts/ux_audit_*
scripts/provider_local.json
scripts/settings_payload.json

# OpenCode project-level config (agents use global ~/.config/opencode/agents/)
.opencode/
!.opencode/oh-my-openagent.jsonc

# Local unsloth studio scratch/working dir (machine-local, unrelated to this project)
unsloth_compiled_cache/

# Local verification and audit scratch space
.local/
.audit-worktrees/

# oh-my-openagent (OpenCode) runtime state: session-continuation blobs and
# working plan docs, machine-local like .opencode/ above
.omo/

# Scheduled-review runtime artifacts (machine-local; log.md, archive/ and
# last-review-date.txt stay tracked, the wrapper script commits them)
skill-observations/last-scheduled-review-output.txt
skill-observations/last-scheduled-review-report.md
skill-observations/last-scheduled-review-status.txt
skill-observations/claudemd-backup.md
skill-observations/claudemd-staged*.md
