# E2E Test Skill for WhisperDeck Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `.claude/skills/e2e-test-app/SKILL.md`, an agent-drivable end-to-end test covering every WhisperDeck feature, runnable by a weak model via Playwright MCP against a skill-owned, isolated server instance.

**Architecture:** One skill file with a setup section (spawn `app.py` on a throwaway data dir/port, launch Chromium with fake media devices), 16 numbered scenarios (deterministic Playwright steps + hard pass/fail checks, no fuzzy quality judgment), and a teardown + reporting section. Built incrementally, each task's scenarios validated by actually running them against the real local dev stack (Moonshine + Lemonade) before moving on.

**Tech Stack:** Playwright MCP tools, FastAPI app (`app.py`), Moonshine (local STT), Lemonade server at `localhost:13305` (local LLM, model `gpt-oss-20b-mxfp4-GGUF`), PowerShell (Windows environment).

## Global Constraints

- Server isolation: every run uses `WHISPERDECK_DATA_DIR=<temp dir>` and `PORT=9782` — never the user's real `data/` dir or default port 9781. (spec: "Process isolation")
- No cloud providers, no API keys, no network cost. Local/keyless backends only: Moonshine (STT), installed diarization backend (pyannote if present, else heuristic), Lemonade at `http://localhost:13305/v1` (LLM). (spec: "Backend configuration")
- Hybrid determinism: every step is either a scripted Playwright action with an exact wait/poll condition, or a DOM selector/text presence check. No step asks the agent to judge visual quality or "did this improve." (spec: "Determinism model")
- Fuzzy LLM outcomes (correction, context refinement) are checked as: job status == `complete`, output non-empty, output != input. Never "is it better." (spec: "Determinism model")
- Failure handling: each step logs `PASS | FAIL | SKIPPED(reason)` and the skill continues past failures — never aborts the run on one bad step. (spec: "Failure handling & reporting")
- Fixture: `tests/fixtures/e2e_multispeaker.<ext>`, user-provided, ~2-3 min, 2-3 speakers. Scenarios 10-12 (diarization, speaker rename/retag, voice bank) are blocked until it lands — build them, but mark them explicitly non-runnable until the file exists. (spec: "Open dependency")

---

## File Structure

- Create: `.claude/skills/e2e-test-app/SKILL.md` — the entire deliverable. Single file (skills in this repo's plugin ecosystem are self-contained markdown; no code files needed since all "test code" is Playwright MCP tool-call sequences the agent executes directly).
- No other repo files change. `tests/fixtures/e2e_multispeaker.<ext>` is a data file the user supplies separately, not something this plan creates.

---

## Task 1: Skill scaffold, setup, and teardown

**Files:**
- Create: `.claude/skills/e2e-test-app/SKILL.md`

**Interfaces:**
- Produces: the skill's YAML frontmatter (`name`, `description`) and a "Setup" / "Teardown" section every later task's scenarios assume already ran. Later tasks assume the server is reachable at `http://localhost:9782` and Chromium is already launched with fake-media flags.

- [ ] **Step 1: Create the skill directory and file with frontmatter + setup section**

```markdown
---
name: e2e-test-app
description: Full end-to-end feature test of WhisperDeck via Playwright MCP against an isolated local server instance (Moonshine STT, local diarization, Lemonade LLM). Use when asked to run the e2e test, verify all features work, or regression-test WhisperDeck before a release.
---

# WhisperDeck End-to-End Test

Drives every feature of WhisperDeck through a real browser against a
throwaway server instance. Local/keyless backends only — no API keys, no
network cost. Each step is either a scripted Playwright action with an
exact check, or a DOM assertion — never a subjective judgment call.

Report format: after every scenario, emit one line:
`[PASS|FAIL|SKIPPED(reason)] Scenario N: <name>`
Continue to the next scenario on failure. Never abort the run early.

## Setup

1. Pick an isolated data dir and port. On Windows PowerShell:

```powershell
$env:WHISPERDECK_DATA_DIR = "$env:TEMP\whisperdeck-e2e-$(Get-Random)"
$env:PORT = "9782"
New-Item -ItemType Directory -Force -Path $env:WHISPERDECK_DATA_DIR | Out-Null
```

2. Start the server as a background process from the repo root:

```powershell
$proc = Start-Process -FilePath ".venv\Scripts\python.exe" -ArgumentList "app.py" `
  -WorkingDirectory (Get-Location) -PassThru -WindowStyle Hidden
```

If `.venv\Scripts\python.exe` doesn't exist, use `python.exe` instead.
Record `$proc.Id` — teardown needs it.

3. Poll until healthy (max 30s):

```powershell
$ready = $false
for ($i = 0; $i -lt 30; $i++) {
  try {
    $r = Invoke-RestMethod -Uri "http://localhost:9782/api/health" -TimeoutSec 2
    if ($r) { $ready = $true; break }
  } catch {}
  Start-Sleep -Seconds 1
}
```

If `$ready` is `false` after 30s: report `FAIL: server did not become healthy`
and stop the whole run (this is the one case where aborting is correct —
nothing downstream can run without a server).

4. Check what's actually available before running scenarios:

```powershell
Invoke-RestMethod -Uri "http://localhost:9782/api/providers"
```

Record: is a diarization backend beyond heuristic installed? Is
`http://localhost:13305/v1/models` reachable (Lemonade)? Use this to decide
which scenarios run at full fidelity vs. get marked
`SKIPPED(backend unavailable)`.

5. Launch Chromium via the Playwright MCP browser tool with fake media
   device flags, pointed at `http://localhost:9782`:
   - `--use-fake-device-for-media-stream`
   - `--use-file-for-fake-audio-capture=<repo-root>\tests\fixtures\e2e_multispeaker.wav`
   (If the fixture isn't present yet, omit this flag — live-capture
   scenario 6 will be `SKIPPED(no fixture)`.)

6. Navigate to `http://localhost:9782/` and confirm the page loads (title
   or root element present) before starting Scenario 1.

## Teardown

Run this after all scenarios, even if some failed:

```powershell
Stop-Process -Id $proc.Id -Force -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force $env:WHISPERDECK_DATA_DIR -ErrorAction SilentlyContinue
```

Close the Playwright browser session.

## Final report

After teardown, print the full list of `[PASS|FAIL|SKIPPED]` lines from
every scenario as a single summary table.
```

- [ ] **Step 2: Verify the file is valid markdown with correct frontmatter**

Run: `Get-Content .claude\skills\e2e-test-app\SKILL.md -TotalCount 5`
Expected: shows the `---` frontmatter block with `name: e2e-test-app` and a `description:` line — confirms the file isn't truncated and frontmatter parses as YAML (no tabs, no unescaped colons in the description).

- [ ] **Step 3: Dry-run the setup section manually**

Run the PowerShell commands from Step 1 literally (steps 1-4, skip step 5-6 which need Playwright) in the actual repo:

```powershell
$env:WHISPERDECK_DATA_DIR = "$env:TEMP\whisperdeck-e2e-test1"
$env:PORT = "9782"
New-Item -ItemType Directory -Force -Path $env:WHISPERDECK_DATA_DIR | Out-Null
$proc = Start-Process -FilePath ".venv\Scripts\python.exe" -ArgumentList "app.py" -WorkingDirectory (Get-Location) -PassThru -WindowStyle Hidden
Start-Sleep -Seconds 3
Invoke-RestMethod -Uri "http://localhost:9782/api/health"
Invoke-RestMethod -Uri "http://localhost:9782/api/providers"
Stop-Process -Id $proc.Id -Force
Remove-Item -Recurse -Force $env:WHISPERDECK_DATA_DIR
```

Expected: `/api/health` returns a healthy response, `/api/providers` lists
providers including `moonshine`, the process stops cleanly, and
`$env:WHISPERDECK_DATA_DIR` no longer exists on disk. If the port is
already in use or the venv path is wrong, fix the setup section now before
building on it.

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/e2e-test-app/SKILL.md
git commit -m "feat: scaffold e2e-test-app skill with setup/teardown"
```

---

## Task 2: Scenarios 1-4 (auth, settings, hotwords, providers)

**Files:**
- Modify: `.claude/skills/e2e-test-app/SKILL.md`

**Interfaces:**
- Consumes: server running at `http://localhost:9782`, browser navigated to `/`, from Task 1's Setup.
- Produces: a registered test user session (cookie-based) that scenarios 5+ assume is already logged in.

- [ ] **Step 1: Append Scenarios 1-4 to the skill file**

```markdown
## Scenario 1: Auth (register / login / logout / session persistence)

1. Navigate to `http://localhost:9782/`. If a login/register form is shown,
   fill username `e2e_test_user`, password `e2e_test_pass_123`, submit
   register.
   - Check: `GET http://localhost:9782/api/me` (via browser fetch or
     Playwright network inspection) returns 200 with
     `"username": "e2e_test_user"`.
2. Reload the page.
   - Check: still authenticated — no login form shown, `/api/me` still
     200. This is the session-persistence check.
3. Trigger logout (find and click the logout control in the UI).
   - Check: `/api/me` now returns 401, login form is shown again.
4. Log back in with the same credentials for the rest of the run.
   - Check: `/api/me` 200 again with the same username.

Report: `[PASS|FAIL] Scenario 1: Auth`

## Scenario 2: Settings (view / update / persists)

1. Open the settings panel in the UI.
   - Check: current settings load without error (values populate, no
     error toast).
2. Change one setting (e.g. a numeric field like max concurrent chunks) to
   a new value and save.
   - Check: a success indicator appears (toast, or field reflects saved
     state).
3. Reload the page and reopen settings.
   - Check: the changed value is still the new value, not reverted to
     default.

Report: `[PASS|FAIL] Scenario 2: Settings`

## Scenario 3: Hotwords (add / list / delete / dedup)

1. Open the hotwords panel.
2. Add a hotword, e.g. `Kubernetes`.
   - Check: it now appears in the hotwords list.
3. Add the same word again in a different case, e.g. `kubernetes`.
   - Check: the list still shows only one entry for it (case-insensitive
     dedup) — confirms the fix at `services/hotwords.py`'s
     `add_hotword()` still holds.
4. Delete the hotword.
   - Check: it no longer appears in the list.

Report: `[PASS|FAIL] Scenario 3: Hotwords`

## Scenario 4: Providers panel (switch / save config / models load)

1. Open the providers/services panel.
   - Check: Moonshine appears in the provider list.
2. Configure the local LLM provider: set provider `local`, `api_url` to
   `http://localhost:13305/v1`, model `gpt-oss-20b-mxfp4-GGUF`. Save.
   - Check: save succeeds (no error toast); reload the panel and confirm
     the saved `api_url` and model persisted.
3. Select Moonshine as the active transcription provider (default anyway,
   but explicitly confirm) and check its model list loads without error.

Report: `[PASS|FAIL] Scenario 4: Providers`

If `http://localhost:13305/v1/models` was unreachable in Setup step 4,
still attempt step 2 (saving config doesn't require the server to be up)
but note `SKIPPED(Lemonade unreachable, config saved but unverified live)`
instead of a hard PASS on the "loads without error" sub-check.
```

- [ ] **Step 2: Verify markdown structure**

Run: `Select-String -Path .claude\skills\e2e-test-app\SKILL.md -Pattern "^## Scenario"`
Expected: shows `Scenario 1` through `Scenario 4` (plus none higher yet) — confirms headers are well-formed and in order.

- [ ] **Step 3: Dry-run Scenarios 1-4 against the real local server**

Using the setup from Task 1 (server running on 9782, browser navigated to
it), manually execute Scenarios 1-4 via Playwright MCP tools (or by hand
in a browser pointed at `localhost:9782` if Playwright MCP isn't
available in this session) to confirm every UI element referenced
("logout control", "hotwords panel", "providers panel") actually exists
with that description. Fix the scenario text now if any referenced
element doesn't match the real UI (e.g. update to the actual button
label/selector found in `static/index.html` or `static/rack.js`).

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/e2e-test-app/SKILL.md
git commit -m "feat: add e2e scenarios 1-4 (auth, settings, hotwords, providers)"
```

---

## Task 3: Scenarios 5-9 (transcribe upload, live capture, transcript CRUD, cancel/resume/retry, retranscribe)

**Files:**
- Modify: `.claude/skills/e2e-test-app/SKILL.md`

**Interfaces:**
- Consumes: logged-in session from Scenario 1; Moonshine confirmed available from Setup step 4.
- Produces: at least one completed transcript in the test DB that Scenarios 10-16 (diarization, speakers, voice bank, summarize, correct, context, jobs) operate on.

- [ ] **Step 1: Append Scenarios 5-9**

```markdown
## Scenario 5: Upload transcribe (file path)

1. Use the file input to upload `tests/fixtures/e2e_multispeaker.<ext>`
   (fall back to the repo's existing `test.mp4` if the multispeaker
   fixture isn't present yet — note `SKIPPED(reduced fixture)` on any
   later scenario that needs multiple speakers, e.g. Scenarios 10-12).
2. Confirm the provider is Moonshine (from Scenario 4) and submit.
3. Poll `GET /api/transcripts/{id}` (or the jobs endpoint) every 3s, up to
   3 minutes, until `status` is `complete` or `failed`.
   - Check: status reaches `complete`.
   - Check: the transcript view renders at least one segment with text.
4. Record this transcript's ID as `$TRANSCRIPT_ID` — later scenarios reuse
   it.

Report: `[PASS|FAIL] Scenario 5: Upload transcribe`

## Scenario 6: Live capture transcribe (fake mic device)

Only runs if Setup launched Chromium with
`--use-file-for-fake-audio-capture`; otherwise `SKIPPED(no fixture/fake
device)`.

1. Trigger the "Live capture" control (the `key-rec` button in the UI).
2. Grant the (fake) microphone permission prompt if one appears.
3. Let it run 5-10 seconds, then stop recording.
4. Confirm a new transcription job is submitted and reaches `complete`
   using the same poll pattern as Scenario 5.

Report: `[PASS|FAIL|SKIPPED(reason)] Scenario 6: Live capture`

## Scenario 7: Transcript list / detail / rename / delete

1. `GET`-equivalent: open the transcript list view.
   - Check: `$TRANSCRIPT_ID` from Scenario 5 appears in the list.
2. Open its detail view.
   - Check: segments/text render.
3. Rename its title via the UI (PATCH under the hood).
   - Check: the new title shows in both detail view and list view after
     reload.
4. Create a second throwaway transcript (repeat Scenario 5's upload with
   `test.mp4`) purely to delete it here, so Scenario 5's main transcript
   survives for later scenarios.
   - Check: after deleting the throwaway one, it no longer appears in the
     list; `$TRANSCRIPT_ID` still does.

Report: `[PASS|FAIL] Scenario 7: Transcript CRUD`

## Scenario 8: Cancel + resume a running job; retry failed chunks

1. Start a new upload transcribe job (third audio submission).
2. Immediately trigger cancel on it via the UI (before it reaches
   `complete`).
   - Check: job status becomes `cancelled` (or the UI's equivalent
     terminal state for a cancelled job).
3. Trigger resume on the same job.
   - Check: it proceeds again and reaches `complete` (or `failed`, in
     which case try "retry failed chunks" next).
4. If any chunk shows `failed` status at any point, trigger "retry failed
   chunks" and confirm those chunks re-run.

Report: `[PASS|FAIL] Scenario 8: Cancel/resume/retry`

## Scenario 9: Retranscribe with a different provider

1. On `$TRANSCRIPT_ID`, trigger "retranscribe" and select a different
   available local provider if one exists (e.g. re-run Moonshine at a
   different model size, since Groq/OpenAI are out of scope per the
   local-only constraint).
2. Poll until `complete`.
   - Check: a new transcription result is produced (job history shows two
     completed runs for this transcript).

Report: `[PASS|FAIL] Scenario 9: Retranscribe`
```

- [ ] **Step 2: Verify markdown structure**

Run: `Select-String -Path .claude\skills\e2e-test-app\SKILL.md -Pattern "^## Scenario"`
Expected: Scenario 1 through Scenario 9 present, in order, no duplicates.

- [ ] **Step 3: Dry-run Scenarios 5-9 against the real local server**

With the server from Task 1's setup still running (or restarted fresh),
actually upload `test.mp4` (or the multispeaker fixture if it has landed
by now) through the real UI and confirm: the job reaches `complete` inside
3 minutes on Moonshine, the cancel/resume/retry controls exist and behave
as described, and retranscribe is reachable from the transcript detail
view. Fix any step whose described UI control doesn't match reality.

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/e2e-test-app/SKILL.md
git commit -m "feat: add e2e scenarios 5-9 (transcribe, capture, CRUD, job control)"
```

---

## Task 4: Scenarios 10-12 (diarization, speaker rename/retag, voice bank) — fixture-gated

**Files:**
- Modify: `.claude/skills/e2e-test-app/SKILL.md`

**Interfaces:**
- Consumes: `$TRANSCRIPT_ID` from Scenario 5 (must have been transcribed from a multi-speaker fixture for these scenarios to be meaningful).
- Produces: at least one named voice profile in the voice bank that Scenario 13+ don't depend on, but that a human reviewer can inspect.

- [ ] **Step 1: Append Scenarios 10-12, explicitly marked fixture-gated**

```markdown
## Scenario 10: Diarization (standalone trigger + rediarize)

Requires `$TRANSCRIPT_ID` to have been transcribed from a genuinely
multi-speaker fixture. If Scenario 5 fell back to `test.mp4` (single
speaker, 3 seconds), mark this `SKIPPED(no multispeaker fixture)` and do
not attempt it — a single-speaker recording cannot validate speaker
separation.

1. Trigger the standalone diarize action on `$TRANSCRIPT_ID`.
2. Poll until the diarization job completes.
   - Check: segments now show more than one distinct speaker label.
3. Trigger "rediarize".
   - Check: it re-runs and completes without error.

Report: `[PASS|FAIL|SKIPPED(reason)] Scenario 10: Diarization`

## Scenario 11: Speaker rename + segment retag

Same fixture gate as Scenario 10.

1. Rename one detected speaker label (e.g. `Speaker 1` -> `Alice`) via the
   UI.
   - Check: the new name appears on all of that speaker's segments in the
     transcript view, not just one.
2. Retag a single segment to a different speaker.
   - Check: that segment now shows the reassigned speaker label, and
     total segment count is unchanged (retagging doesn't create/delete
     segments).

Report: `[PASS|FAIL|SKIPPED(reason)] Scenario 11: Speaker rename/retag`

## Scenario 12: Voice bank (enroll / list / identify / delete)

Same fixture gate as Scenario 10.

1. From a segment belonging to "Alice" (Scenario 11), trigger
   "enroll-speaker" to create a voice bank profile.
   - Check: a new voice profile named `Alice` (or as entered) appears
     under `GET /api/voices`.
2. Upload a second transcript from the same multispeaker fixture (or a
   different clip containing the same voice).
3. Trigger "identify" against the voice bank on this new transcript.
   - Check: at least one segment gets matched/labeled against the
     enrolled `Alice` profile.
4. Delete the enrolled voice profile.
   - Check: it no longer appears under `GET /api/voices`.

Report: `[PASS|FAIL|SKIPPED(reason)] Scenario 12: Voice bank`
```

- [ ] **Step 2: Verify markdown structure**

Run: `Select-String -Path .claude\skills\e2e-test-app\SKILL.md -Pattern "^## Scenario"`
Expected: Scenario 1 through Scenario 12 present in order.

- [ ] **Step 3: Note fixture dependency, do not attempt live dry-run yet**

These three scenarios cannot be dry-run for real until
`tests/fixtures/e2e_multispeaker.<ext>` exists. Skip the live dry-run step
for this task; it happens in Task 6 once the fixture has landed.

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/e2e-test-app/SKILL.md
git commit -m "feat: add e2e scenarios 10-12 (diarization, speakers, voice bank)"
```

---

## Task 5: Scenarios 13-16 (summarize, correction, context, jobs panel) + final report section

**Files:**
- Modify: `.claude/skills/e2e-test-app/SKILL.md`

**Interfaces:**
- Consumes: `$TRANSCRIPT_ID` from Scenario 5; Lemonade reachability flag from Setup step 4.

- [ ] **Step 1: Append Scenarios 13-16**

```markdown
## Scenario 13: Summarize (Lemonade)

Requires Lemonade reachable (Setup step 4); otherwise
`SKIPPED(Lemonade unreachable)`.

1. Trigger "summarize" on `$TRANSCRIPT_ID`.
2. Poll the LLM job status every 5s, up to 5 minutes (local 20B inference
   is slow — do not use a short timeout here).
   - Check: job status reaches `complete`, not `failed`.
   - Check: `GET /api/transcripts/{id}/summary` returns non-empty text.

Report: `[PASS|FAIL|SKIPPED(reason)] Scenario 13: Summarize`

## Scenario 14: Correction pass (Lemonade)

Same Lemonade gate as Scenario 13.

1. Record the transcript's current text (pre-correction) for comparison.
2. Trigger "correct" on `$TRANSCRIPT_ID`.
3. Poll up to 5 minutes.
   - Check: job status reaches `complete`.
   - Check: corrected output is non-empty.
   - Check: corrected output text is not byte-identical to the
     pre-correction text recorded in step 1. (This is a hard check, not a
     quality judgment — it only proves the correction pass ran and
     produced *a* transformation, not that the transformation is good.)

Report: `[PASS|FAIL|SKIPPED(reason)] Scenario 14: Correction`

## Scenario 15: Context refinement

Same Lemonade gate.

1. Add a short context document (a few sentences of relevant background/
   glossary text) to `$TRANSCRIPT_ID` via the context feature.
2. Re-run correction or transcription-refinement with that context
   attached (whichever the UI wires context into).
3. Poll up to 5 minutes.
   - Check: job status reaches `complete`, not `failed`.

Report: `[PASS|FAIL|SKIPPED(reason)] Scenario 15: Context refinement`

## Scenario 16: Jobs panel (list / cancel / rerun)

1. Open the jobs panel.
   - Check: it lists the jobs created by Scenarios 5-15 (upload,
     diarize, summarize, correct, etc.) with their statuses.
2. Start one more transcribe job specifically to cancel it here (don't
   reuse a job another scenario still needs).
   - Check: cancel from the jobs panel works and updates its status.
3. Trigger "rerun" on any completed job.
   - Check: a new job entry appears and reaches a terminal status
     (`complete` or `failed`).

Report: `[PASS|FAIL] Scenario 16: Jobs panel`
```

- [ ] **Step 2: Append the final summary format reminder at the end of the file**

```markdown
## End of run

Print every scenario's report line in order (1 through 16) as the final
output of this skill, then confirm Teardown ran.
```

- [ ] **Step 3: Verify markdown structure end-to-end**

Run: `Select-String -Path .claude\skills\e2e-test-app\SKILL.md -Pattern "^## Scenario"`
Expected: Scenario 1 through Scenario 16, each exactly once, in ascending order.

- [ ] **Step 4: Dry-run Scenarios 13, 14, 16 against the real local server + Lemonade**

With the server running and Lemonade reachable at `localhost:13305`
(confirmed working per the earlier Lemonade config work), actually
trigger summarize and correct on a real transcript and confirm: jobs
reach `complete` within 5 minutes, `/api/transcripts/{id}/summary`
returns text, and corrected text differs from the input. If gpt-oss-20b
takes longer than 5 minutes on this hardware for a short transcript,
raise the poll timeout in the scenario text to whatever was actually
observed, plus margin. Scenario 15 (context refinement) can be dry-run
too if time allows, but 13/14/16 are the priority since they're pure
Lemonade-integration risk.

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/e2e-test-app/SKILL.md
git commit -m "feat: add e2e scenarios 13-16 (summarize, correction, context, jobs) and final report"
```

---

## Task 6: Full dry-run once the multispeaker fixture lands

**Files:**
- Create: `tests/fixtures/e2e_multispeaker.<ext>` (user-provided, not authored by this task)
- Modify: `.claude/skills/e2e-test-app/SKILL.md` (only if the dry-run surfaces a mismatch between a described UI element and reality)

**Interfaces:**
- Consumes: everything from Tasks 1-5.

- [ ] **Step 1: Confirm the fixture exists and is usable**

Run: `Get-Item tests\fixtures\e2e_multispeaker.*`
Expected: file exists, and its duration is at least ~1 minute with 2+
speakers (spot-check by ear or by looking at existing diarization output
after a manual transcribe).

- [ ] **Step 2: Update Scenario 5's file path reference if the fixture's actual extension differs from the placeholder used in Tasks 1-5**

If the fixture is e.g. `.m4a` instead of `.wav`, update every reference to
`e2e_multispeaker.<ext>` in the skill file to the real filename.

- [ ] **Step 3: Run the full skill end-to-end via Playwright MCP**

Invoke `.claude/skills/e2e-test-app/SKILL.md` for real, start to finish,
against the actual local stack (Moonshine + installed diarization backend
+ Lemonade). Capture the final PASS/FAIL/SKIPPED report.

- [ ] **Step 4: Fix any scenario whose described UI element, selector, or timing doesn't match observed reality**

For each FAIL that turns out to be a skill-authoring bug rather than a
real app bug (e.g. wrong button label, timeout too short, wrong endpoint
path), correct the relevant scenario text in the skill file. For each FAIL
that is a real app bug, do not fix the app here — report it to the user
as a finding; this plan's scope is the test skill, not app fixes.

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/e2e-test-app/SKILL.md tests/fixtures/e2e_multispeaker.*
git commit -m "test: validate e2e-test-app skill against real multispeaker fixture"
```

---

## Self-Review Notes

- **Spec coverage:** all 16 scenarios from the spec's "Scenario coverage"
  section map 1:1 to Tasks 2-5. Process isolation (Task 1), determinism
  model (every scenario's Check lines are hard DOM/status checks, no
  "judge if this looks right"), fixture gating (Task 4 + Task 6), and
  failure handling / final report (Task 1 teardown section + Task 5 Step
  2) are all covered.
- **Placeholder scan:** no TBD/TODO; every scenario step names the exact
  UI action and exact check. The one genuinely open item (fixture file
  extension) is called out explicitly as a Task 6 follow-up, not hidden.
- **Type/interface consistency:** `$TRANSCRIPT_ID` introduced in Scenario
  5 (Task 3) is the only cross-scenario variable, referenced consistently
  in Scenarios 7-16 by that same name.
