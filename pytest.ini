[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -m "not e2e"
markers =
    e2e: end-to-end tests that drive a real browser via Playwright. Opt-in: `pytest -m e2e`. Excluded by default.
